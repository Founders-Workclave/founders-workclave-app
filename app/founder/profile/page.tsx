import SettingsPage from "@/components/settingsProfile";
import FounderLayout from "@/layout/founder";

const PrdComp = () => {
  return (
    <FounderLayout pageTitle="Settings" pageText="">
      <SettingsPage />
    </FounderLayout>
  );
};

export default PrdComp;
